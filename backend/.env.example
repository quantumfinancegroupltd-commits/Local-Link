# Copy to .env and set at least DATABASE_URL.
# Backend loads .env automatically (dotenv) for migrate and dev.

# Required for DB (migrate + API)
DATABASE_URL=postgresql://USER@localhost:5432/locallink

# Optional: port (default 4000)
# PORT=4000

# Optional: JWT (default dev secret; must set in production)
# JWT_SECRET=your-secret

# Production-only: CORS_ORIGINS, ADMIN_BOOTSTRAP_SECRET, PAYSTACK_*, etc.
# See config.js for full list.

# Password reset (and other emails) — required for "forgot password" to actually send.
# Without these, the API still returns "email sent" (to avoid account enumeration) but no email is delivered.
# Use a transactional provider (SendGrid, Mailgun, Resend, SES) or your mail server.
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=LocalLink <no-reply@yourdomain.com>
