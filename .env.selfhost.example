# Copy this to .env on the server (same directory as docker-compose.selfhost.yml).
# Do not commit .env — it is gitignored.
# Generate strong values, e.g.: openssl rand -hex 32

JWT_SECRET=
ADMIN_BOOTSTRAP_SECRET=
